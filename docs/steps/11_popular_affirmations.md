# Шаг 11: Вирусные (популярные) аффирмации

## 🎯 Цель
Сгенерировать короткие афористичные продолжения для каждой аффирмации в `affirmations_new.script`, чтобы использовать их как вирусные тизеры.

---

## 🧩 Исходные данные
- Таблица `affirmations_new`: поля `id`, `subcategory_id`, `coach_id`, `position`, `script`, `popular_aff`.
- Диапазоны фильтров Берём из `config.yaml` (`range.categories`, `range.subcategories`, `range.positions`, `versions`, `languages`).
- Промпт: `docs/agents/popular_affirmation.md`.
- Параметры LLM: `model=ids.popular_aff_model` (по умолчанию `gpt-5`), `response_schema=PopularAffirmationResponse`.

---

## ⚙️ Конфигурация
```yaml
steps:
  popular_affirmations: true   # новый флаг
threads_popular_aff: 8         # опционально, иначе используется общее значение
```
- Используются стандартные retry-настройки (`retry.*`) и лимиты (`rate_limits.openai`).

---

## 🚀 Логика выполнения
1. Загружаем категории → подкатегории → наставников → аффирмации, применяя диапазоны из `config.yaml`.
2. Для каждой записи берём `affirmations_new.script`, парсим JSON вида `{"female":{"EN":{"title","script"},...},"male":{...}}`, а заголовок каждой аффирмации (`title`) подтягиваем из `subcategories.localization` для нужного пола/языка (если локализация отсутствует — используем `script.title`).
3. Проходим по языкам из `config.languages`. Если для пола/языка уже есть значение в `popular_aff`, при обработке языка перезаписываем; остальные языки внутри поля не трогаем.
4. Для каждой комбинации пола (`female` → `male`), языка, записи:
   - Логируем короткое сообщение (категория/подкатегория/коуч/позиция/пол/язык).
   - Отправляем запрос в LLM с промптом `popular_affirmation.md` и JSON:
     ```json
     {
       "title": "...",
       "script": "..."
     }
     ```
   - LLM возвращает одну строку (4–8 слов), которая звучит как завершённая мантра.
5. Обновляем `popular_aff`:
   ```json
   {
     "female": {"EN": "…", "RU": "…"},
     "male":   {"EN": "…"}
   }
   ```
   - Перед записью объединяем с существующим JSON, затирая только обработанные языки для конкретного пола.
6. Сохраняем в `affirmations_new.popular_aff`. Журналируем успех и ошибки.

---

## 📦 Выходные данные
- Поле `affirmations_new.popular_aff`, содержащее короткие вирусные мантры для каждого пола и языка.

---

## 🧾 Возможные ошибки
| Ошибка | Причина | Действие |
| --- | --- | --- |
| Invalid script JSON | Повреждён `affirmations_new.script` | Пропустить запись и записать предупреждение |
| LLM request failed | Сетевые/429 | Повтор через `RetryableStepError` |
| DB update failed | Ошибка Supabase | Обернуть в `RetryableStepError`, чтобы job повторился |

---

## ✅ Критерии завершения
- Для всех выбранных аффирмаций заполнены вирусные строки в `popular_aff` (по заданным языкам/полам).
- Логи содержат наглядный прогресс (что обрабатываем, сколько осталось).
- Нет критических ошибок в `logs/last_run.log`.
