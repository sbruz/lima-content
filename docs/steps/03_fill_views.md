# Шаг 3: Генерация `views`

## 🎯 Цель
Проставить количественные показатели просмотров (`views`) для подкатегорий в Supabase по заданной формуле, чтобы имитировать пользовательскую активность.

---

## 🧩 Исходные данные
- Таблица Supabase: `categories`
  - Поля: `id`, `name`, `position`
- Таблица Supabase: `subcategories`
  - Поля: `id`, `category_id`, `position`, `views`
  - Дополнительно: `ready` — статус шага 2; **не изменяется** на этом шаге.
- Конфигурация `config.yaml`: диапазоны `range.categories`, `range.subcategories`, опциональные параметры формулы.

> **Pydantic:** для вычислений и валидации шаг использует собственные Pydantic-модели. Модели других шагов не переиспользуем и не изменяем.

---

## ⚙️ Конфигурация
Пример секции в `config.yaml`:
```yaml
steps:
  fill_views: true

views:
  base_min: 200     # базовый коэффициент
  base_max: 1000    # верхний доп. диапазон
  seed: 1000        # фиксируем генератор случайных чисел
```
Диапазоны:
```yaml
range:
  categories: [1, -1]
  subcategories: [1, -1]
```

---

## 🚀 Логика выполнения
1. Загрузить категории (учитывая диапазон) и связанные подкатегории.
2. Для каждой подкатегории вычислить `views` по формуле (например, из PRD):
   ```
   views ∈ [ (10 - cat_pos) * base_min + (sub_pos - 10) * 100 ;
            (10 - cat_pos) * base_min + (sub_pos - 10) * 100 + base_max ]
   ```
3. Использовать генератор случайных чисел с фиксированным `seed` (если задан).
4. Записать значения в `subcategories.views`.
5. Логировать длительность и статистику (мин/макс/среднее для шага).
6. При необходимости добавить/обновить зависимости шага (`requirements.txt`).

> Можно запускать в многопоточном режиме по категориям (аналогично Шагу 2).

---

## 📦 Выходные данные
- Обновлённое поле `views` в таблице `subcategories`.
- Консольный лог: начало/конец шага, обработанные категории, суммарное время.
- Структурированный лог (`logs/last_run.log`): детальная статистика и возможные ошибки.

---

## 🧾 Возможные ошибки
| Ошибка | Причина | Решение |
|--------|---------|---------|
| `FatalStepError: Failed to load categories` | Supabase недоступен или неверный URL | Проверить `.env`, повторить позже |
| `RetryableStepError: Failed to update subcategories` | Ошибка записи в БД | Повторить запуск, проверить права |
| `ValueError: invalid range` | Некорректные параметры формулы/диапазона | Проверить конфигурацию |

---

## ✅ Критерии завершения
- Все выбранные подкатегории получили значение `views`.
- Шаг завершился без ошибок, в логах отмечено количество обновлённых записей и время.
- При повторном запуске значения пересчитываются детерминированно (при фиксированном seed).
