# Шаг 4: Генерация аффирмаций

## 🎯 Цель
Для каждой подкатегории и тренера из множества `config.versions` подготовить набор аффирмаций в духе тренера, включающих отдельные сцены для женщин и мужчин, и сохранить их в таблицу `affirmations`.

---

## 🧩 Исходные данные
- Таблица `categories`: `id`, `name`, `position`.
- Таблица `subcategories`: `id`, `category_id`, `name`, `position`.
- Таблица `coaches`: `id`, `coach`, `prompt`.
- Конфигурация `config.yaml`:
  - `range.categories`, `range.subcategories`;
  - `affirmations_per_subcategory`;
  - `versions` — список тренеров, которых нужно обрабатывать;
  - `steps.generate_affirmations`.

> **Pydantic:** шаг использует собственные модели для валидации ответа LLM. Нельзя переиспользовать модели других шагов.

---

## ⚙️ Конфигурация
```yaml
steps:
  generate_affirmations: true

affirmations_per_subcategory: 10
versions: [Roxie, Goddard]
```

---

## 🚀 Логика выполнения
1. Загрузить категории и подкатегории согласно диапазонам `config.range`.
2. Отфильтровать тренеров из `coaches` по списку `config.versions`. Если тренер не найден — зафиксировать ошибку в логе и пропустить.
3. Для каждой тройки (категория → подкатегория → тренер):
   1. Подготовить payload с контекстом категории/подкатегории, промптом тренера и целевым количеством `affirmations_per_subcategory`.
   2. Отправить запрос в LLM (`docs/agents/affirmations_base.md`) и валидировать ответ строгой Pydantic-схемой:
      ```json
      {
        "female": [
          { "affirmation": "...", "scene": "..." }
        ],
        "male": [
          { "affirmation": "...", "scene": "..." }
        ]
      }
      ```
   3. Проверить, что длина списков соответствует `affirmations_per_subcategory`. При расхождении — залогировать предупреждение, но продолжить.
   4. Очистить существующие записи `affirmations` для выбранной подкатегории и тренера, чтобы не накапливать устаревшие данные.
   5. Сформировать новые записи:
      ```json
      {
        "subcategory_id": ...,
        "coach_id": ...,
        "position": <1..N>,
        "affirmation": {
          "female": {"affirmation": "...", "scene": "..."},
          "male":   {"affirmation": "...", "scene": "..."}
        },
        "script": null,
        "music": null
      }
      ```
      и вставить их батчем.
4. В случае ошибок запроса к OpenAI выбрасывать `RetryableStepError` (будет применена политика ретраев).

---

## 📦 Выходные данные
- Таблица `affirmations` заполнена свежими данными для каждой комбинации подкатегория × тренер.
- Поля `script` и `music` обнулены (готовы к дальнейшим шагам).
- Логи содержат:
  - бизнес-события: старт/финиш обработки подкатегории, количество аффирмаций, длительность;
  - технические записи (payload, ошибки) в файле `logs/last_run.log`.

---

## 🧾 Возможные ошибки
| Ошибка | Причина | Действия |
|--------|---------|----------|
| `FatalStepError` при чтении категорий/подкатегорий | недоступен Supabase | проверить `.env`, сеть |
| `RetryableStepError` при вставке в `affirmations` | конфликт или сетевой сбой | пайплайн повторит попытку |
| Ошибка валидации Pydantic | модель вернула неожиданный формат | откорректировать промпт либо схему |

---

## ✅ Критерии завершения
- Для каждой выбранной подкатегории и тренера существует ровно `affirmations_per_subcategory` записей.
- Поля `affirmation` содержат структуру с двумя полами.
- В консоли зафиксированы бизнес-логи с длительностью обработки.
